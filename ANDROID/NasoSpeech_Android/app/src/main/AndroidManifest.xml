<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dod.applicationnaso">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".doctor_dashboard"
            android:label="@string/title_activity_doctor_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".signup" />
        <activity
            android:name=".Forgot_Password"
            android:label="@string/title_activity_forgot__password"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".Hypernasality" />
        <activity android:name=".Articulation" />
        <activity android:name=".Intelligibility" />
        <activity android:name=".VoicingError" />
    </application>

</manifest>